{{- if .Values.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "vigil-controller.fullname" . }}-leader-election-rolebinding
  labels:
    {{- include "vigil-controller.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "vigil-controller.fullname" . }}-leader-election-role
subjects:
- kind: ServiceAccount
  name: {{ include "vigil-controller.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
{{- end }}
